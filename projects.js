document.addEventListener('DOMContentLoaded',()=>{const filters=document.querySelectorAll('.filter');const cards=document.querySelectorAll('.project-card');filters.forEach(filter=>{filter.addEventListener('click',()=>{filters.forEach(f=>f.classList.remove('active'));filter.classList.add('active');const target=filter.dataset.filter;cards.forEach(card=>{const categories=card.dataset.category||'';card.classList.toggle('hidden-card',target!=='all'&&!categories.includes(target))})})});const pos=document.querySelector('.pos-project');if(pos){pos.addEventListener('click',e=>{if(e.button!==0||e.ctrlKey||e.metaKey||e.shiftKey||e.altKey)return;e.preventDefault();document.body.classList.add('pos-launching');const layer=document.getElementById('posLaunch');if(layer)layer.classList.add('active');setTimeout(()=>{window.location.href=pos.getAttribute('href')||'pos.html';},5000);});}});