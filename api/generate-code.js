import crypto from 'node:crypto';const SECRET=process.env.PORTFOLIO_AUTH_SECRET,ADMIN=process.env.PORTFOLIO_ADMIN_KEY,CODE_LIFETIME=7*24*60*60*1000;const sign=v=>crypto.createHmac('sha256',SECRET).update(v).digest('base64url');const random=()=>crypto.randomBytes(5).toString('hex').toUpperCase();export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});if(!SECRET||!ADMIN)return res.status(503).json({error:'Owner authentication is not configured.'});try{const {adminKey,durationSeconds}=typeof req.body==='string'?JSON.parse(req.body||'{}'):(req.body||{}),supplied=String(adminKey||'');if(!supplied||supplied.length!==ADMIN.length||!crypto.timingSafeEqual(Buffer.from(supplied),Buffer.from(ADMIN)))return res.status(403).json({error:'Owner key rejected.'});const duration=Number(durationSeconds);if(!Number.isInteger(duration)||duration<300||duration>604800)return res.status(400).json({error:'Duration must be between 5 minutes and 7 days.'});const codeExp=Date.now()+CODE_LIFETIME,nonce=random(),sig=sign(codeExp+'|'+duration+'|'+nonce).slice(0,10).toUpperCase(),code='EDR-'+codeExp.toString(36).toUpperCase()+'-'+duration.toString(36).toUpperCase()+'-'+nonce+'-'+sig;return res.status(200).json({code,durationSeconds:duration,codeExpiresAt:new Date(codeExp).toISOString()})}catch(e){return res.status(400).json({error:'Invalid request.'})}}